<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} ----- %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>
            C:/Users/new/Documents/GitHub/foo-training/loggingFile.log
        </file>

        <encoder>
            <pattern>%-5level %logger{36} --- %msg%n</pattern>
        </encoder>
    </appender>

<!--    logs showed in the console and file-->
    <logger name="mobi.foo.training.product" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </logger>

<!--    logs showed in console only-->
   <root level="INFO">
        <appender-ref ref="CONSOLE" />
    </root>
</configuration>
